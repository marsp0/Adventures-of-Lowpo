#include "MoveData.hpp"

MoveData::MoveData(bool forward, bool backward, bool left, bool right) : 
					Data(),
					forward(forward), 
					backward(backward), 
					left(left), 
					right(right)
{
	
}