os: linux
sudo: true
jobs:
  include:
    - stage: Build Game
      script: make
    - stage: Build Tests
      script: make test
  # - stage: Run Tests
  #   script: 
  #     - ./out/test
compiler: gcc
language: cpp
notifications: never
before_install:
  - mkdir out
  - sudo apt-get install -y libglm-dev libglfw3-dev libglew-dev